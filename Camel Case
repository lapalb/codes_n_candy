#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    cin>>s;
    int x=s.size();
    int count=1;
    for(int i=0;i<x;i++)
    {
        if(s[i]>=65 && s[i]<=90)
        {
            ++count;
        }
    }
    cout<<count;
}
